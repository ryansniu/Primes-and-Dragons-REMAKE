using UnityEngine;

public class TitleBGAnim : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // orb bounce with color ripple
    // hovering over options changes which orbs bounce
    // border animation spiral color
}
